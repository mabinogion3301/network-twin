# Em desenvolvimento local, deixe vazio (usa o proxy do Vite para localhost:3000).
# Em produção, defina a URL completa da sua API backend (sem barra no final):
VITE_API_URL=https://sua-api.exemplo.com
